| Strategy         | Queries | Problem               |
| ---------------- | ------- | --------------------- |
| Lazy Loading     | 1 + N   | N+1 queries           |
| Explicit Loading | 1 + N   | N+1 queries           |
| Eager Loading    | 1       | Cartesian explosion   |
| Split Query      | Few     | Usually safe          |
| Projection       | 1       | Usually best for APIs |


Keyword	Used For	Risk
Include	- Eager loading	Cartesian explosion
ThenInclude	 - Nested eager loading	Same
Entry -	Explicit loading	N+1 if misused
Collection -	Explicit load list nav	N+1 if loop
Reference - 	Explicit load single nav	N+1 if loop
Load -	Executes explicit load	N+1 risk
AsSplitQuery -	Avoid explosion	Safe
Select -	Projection	Best
AsNoTracking -	Read-only optimization	Safe

authorization is for settting the claims policies(subscription,isveriifed) -(what can you do)and authentication is responsible for validating token (who are you)
authentication
-validates claims,tokens etc. if everything fine User.Identity.IsAuthenticated = true
authorization
-authorization is for some endpoints where only login requred which authoriza us isauthenticated and can also create roles like not only admin user but premium user etc.
-checks are you allowed according to your claims and you satisfy policy and isauthenticated is true or not

-di will onl handle controllers,middleware,minimalapi and classes registered under di
-why we did configuration done using fluent api configuration like
Advanced configurations like:

Backing fields

Composite keys

Owned types

Shadow properties

Table renaming

Composite foreign keys
- in DDD infrastructure is responsible for ef core configuration Domain
Application
Infrastructure (EF Core)
API
Complex relationships need Fluent API
Fluent API overrides Data Annotations.
Fluent API gives:

More control

More flexibility

Cleaner separation
Fluent API is a method-chaining configuration style used in EF Core to define how your domain classes map to the database.

It is more powerful and flexible than attributes.

Property Access is setting using setter
eg modelBuilder.Entity<Gig>()
    .Property(g => g.Title)
    .UsePropertyAccessMode(PropertyAccessMode.Property);
Field Access Mode it uses backing field to update setter is not called
prefer field is option where if it loads ddata from db uses field only but during normal setting there property works
eg modelBuilder.Entity<Gig>()
    .Property(g => g.Title)
    .HasField("_title")
    .UsePropertyAccessMode(PropertyAccessMode.PreferField);
-inheriting from controller base means
it gives access to properties like return ok , bad request and other routing etc.
using controller base + [apicontroller] already binds dto automatically to body ot whatever
give access to httpcontext
-by default when postlike problem is there we learned that when we do add and when we update primary key be it composite primary key it takes it as update because passing primary key to add says it that it already exist so it updates it which makes state modified and we encouter the problem which states that:
The database operation was expected to affect 1 row(s), but actually affected 0 row(s).
-in database owns many says its dependent we can access the table using ownner but in hasmany we can access the table without using or using owner identity has many doens not ahve strict rules unless mentioned
-OwnsMany represents dependent objects whose lifecycle is tied to the owner. HasMany represents separate entities that can exist and be queried independently.
DESING
REPO(access the data and add it to db)
service uses repo function to add it do the db
so here forign key is for rules basically we can add cascading rules and everythig but it doesn't heplp in quick search like indexes?
-http verb is for character we need to write logic for idempotency and like cachingis by default can be done to get request so http verb is mainly for character but we can write logic on basis of verb
-idempotency
-n+1

Designed Domain layer + Application layer separation

Used EF Core Owned Entities

Solved composite key issues

Handled backing fields properly

Implemented JWT authentication

Understood claims vs roles vs authorization

Dealt with idempotency

Understood Change Tracker

Used AsNoTracking properly

Implemented Domain Events

 

-refresh token is send to client with access token 
--after login the db stores refresh token in hash 
--sends the normal without hashed refresh token
--this token is stored in cookies with only send over https network with only same site can send cookies with request also it doesn't allow scritp or opening the cookies folder to see it
--now with all the request it gets send and once
--logic of refresh token to get access token is writeen on frontend

-valuetask is allocating memory in stack when its returned synchrnously

-In LINQ, most query operators like:

Where()

Select()

OrderBy()

Take()

Skip()

use deferred execution.

-set<> this is makes a object instance reasonly 
-Set<T>() looks up the correct one from internal dictionary.

-why to check the user before checking refresh token
Scenario 1 — User was deleted

Imagine:

User logs in → gets refresh token.

Admin deletes user.

Attacker still has refresh token.

Attacker calls /refresh.

Right now MusicHub has:

JWT + Refresh Token rotation ✅

CRUD posts ✅

Reporting + moderation ✅

Soft delete ✅

Pagination (basic) ✅

added exception handling middleware

TEST ADMIN CREDENTIALS
{ "email": "admin@test.com", "password": "admin@1234" }
