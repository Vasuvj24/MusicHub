Strategy	Queries	Result Shape	Risk
Lazy	1 + N	Clean per query	? N+1
Explicit	1 + N	Clean per query	? N+1
Eager Single	1	Duplicated rows	? Cartesian explosion
SplitQuery	Few	Clean	? Safe
Projection	1 optimized	Clean	?? Best

Keyword	Used For	Risk
Include	Eager loading	Cartesian explosion
ThenInclude	Nested eager loading	Same
Entry	Explicit loading	N+1 if misused
Collection	Explicit load list nav	N+1 if loop
Reference	Explicit load single nav	N+1 if loop
Load	Executes explicit load	N+1 risk
AsSplitQuery	Avoid explosion	Safe
Select	Projection	Best
AsNoTracking	Read-only optimization	Safe

authorization is for settting the claims policies(subscription,isveriifed) -(what can you do)and authentication is responsible for validating token (who are you)
authentication
-validates claims,tokens etc. if everything fine User.Identity.IsAuthenticated = true
authorization
-checks are you allowed according to your claims and you satisfy policy and isauthenticated is true or not

-di will onl handle controllers,middleware,minimalapi and classes registered under di
-why we did configuration done using fluent api configuration like
Advanced configurations like:

Backing fields

Composite keys

Owned types

Shadow properties

Table renaming

Composite foreign keys
- in DDD infrastructure is responsible for ef core configuration Domain
Application
Infrastructure (EF Core)
API
Complex relationships need Fluent API
Fluent API overrides Data Annotations.
Fluent API gives:

More control

More flexibility

Cleaner separation
Fluent API is a method-chaining configuration style used in EF Core to define how your domain classes map to the database.

It is more powerful and flexible than attributes.

Property Access is setting using setter
eg modelBuilder.Entity<Gig>()
    .Property(g => g.Title)
    .UsePropertyAccessMode(PropertyAccessMode.Property);
Field Access Mode it uses backing field to update setter is not called
prefer field is option where if it loads ddata from db uses field only but during normal setting there property works
eg modelBuilder.Entity<Gig>()
    .Property(g => g.Title)
    .HasField("_title")
    .UsePropertyAccessMode(PropertyAccessMode.PreferField);
-inheriting from controller base means
it gives access to properties like return ok , bad request and other routing etc.
using controller base + [apicontroller] already binds dto automatically to body ot whatever
give access to httpcontext
-by default when postlike problem is there we learned that when we do add and when we update primary key be it composite primary key it takes it as update because passing primary key to add says it that it already exist so it updates it which makes state modified and we encouter the problem which states that:
The database operation was expected to affect 1 row(s), but actually affected 0 row(s).
-in database owns many says its dependent we can access the table using ownner but in hasmany we can access the table without using or using owner identity has many doens not ahve strict rules unless mentioned